// TODO : fileServer to read manifests + tests for insert

import { readFileToObject, toUrl } from "#fileServer/utils.js";

const manifest2ValidUri = { uri: toUrl };


const manifest2ValidReferenced = "";

